import "./admin.css";

export const metadata = {
  title: "لوحة التحكم — Tiba Admin",
};

export default function AdminRootLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
